
================================================================================
📘 README: YOLO 2-Class Data Processing and Testing System
================================================================================

ชุดไฟล์นี้ใช้สำหรับ:
- คัดเลือกข้อมูล (01_SELECT_DATA.ipynb)
- เตรียมชุดข้อมูลสำหรับฝึก YOLO (02_CREATE_DATA_FOR_TRAIN_YOLO.ipynb)
- เตรียมชุดข้อมูลสำหรับฝึก YOLO แบบ 2 คลาส (02_CREATE_DATA_FOR_TRAIN_YOLO_2CLASS.ipynb)
- ทดสอบระบบหลังจากการเทรน (03.Test_System.ipynb)

--------------------------------------------------------------------------------
🟦 01_SELECT_DATA.ipynb — เลือกข้อมูลที่ต้องการ
--------------------------------------------------------------------------------
หน้าที่:
- อ่านไฟล์ข้อมูลที่มี label
- เลือกเฉพาะบางคลาสที่ต้องการ
- บันทึกไฟล์ผลลัพธ์เป็น .csv หรือ .txt สำหรับใช้ในขั้นตอนต่อไป

การทำงานหลัก:
- กำหนด path ไปยังไฟล์ข้อมูล
- ระบุชื่อคลาสที่ต้องการคัดเลือก
- Export ไฟล์ผลลัพธ์

สิ่งที่ต้องแก้ไขได้:
- path ของไฟล์ต้นฉบับ
- ชื่อคลาสที่ต้องการเลือก

--------------------------------------------------------------------------------
🟦 02_CREATE_DATA_FOR_TRAIN_YOLO.ipynb — สร้างชุดข้อมูล YOLO (7-Class)
--------------------------------------------------------------------------------
หน้าที่:
- สร้างโครงสร้างโฟลเดอร์สำหรับ YOLO: images/train, images/val, labels/train, labels/val
- แยกข้อมูล Train/Test ตามสัดส่วนที่กำหนด
- จัดเก็บไฟล์ภาพและ label ให้อยู่ในรูปแบบที่ YOLO ต้องการ

การทำงานหลัก:
- อ่านไฟล์จาก list ที่คัดเลือกมา
- คัดลอกไฟล์ภาพและไฟล์ label ไปยังโฟลเดอร์ที่ถูกต้อง
- แบ่ง train/test ด้วย random split

สิ่งที่ต้องแก้ไขได้:
- Path input/output
- อัตราส่วน train/test split

--------------------------------------------------------------------------------
🟦 02_CREATE_DATA_FOR_TRAIN_YOLO_2CLASS.ipynb — สร้างชุดข้อมูล YOLO (2-Class)
--------------------------------------------------------------------------------
หน้าที่:
- คล้ายกับ 02_CREATE_DATA_FOR_TRAIN_YOLO แต่รองรับ 2 คลาสพร้อมกัน
- รวมไฟล์ label ของคลาสที่เลือกเข้าด้วยกันในชุดฝึก

การทำงานหลัก:
- อ่านข้อมูลทั้งสองคลาส
- รวมเข้าด้วยกันก่อนทำ Train/Test split
- สร้างโครงสร้างโฟลเดอร์ใหม่ตาม YOLO format

สิ่งที่ต้องแก้ไขได้:
- เลือก 2 คลาสที่ต้องการรวม
- กำหนด path สำหรับข้อมูลของแต่ละคลาส
- กำหนด output path

--------------------------------------------------------------------------------
🟦 03.Test_System.ipynb — ทดสอบระบบ
--------------------------------------------------------------------------------
หน้าที่:
- โหลดโมเดล YOLO ที่เทรนแล้ว
- รันตรวจจับ (inference) บนชุดภาพใหม่
- แสดงผลภาพที่มี bounding boxes

การทำงานหลัก:
- โหลด weight (.pt) ของโมเดล
- ทำ prediction บนรูป
- แสดงผลการตรวจจับ

สิ่งที่ต้องแก้ไขได้:
- Path ไปยัง weight ของโมเดล YOLO
- Path ของภาพที่ต้องการทดสอบ

================================================================================
📂 โครงสร้างโฟลเดอร์ (หลัง Setup แล้ว)
================================================================================
ROOT/
│
├── 01_SELECT_DATA.ipynb
├── 02_CREATE_DATA_FOR_TRAIN_YOLO.ipynb
├── 02_CREATE_DATA_FOR_TRAIN_YOLO_2CLASS.ipynb
├── 03.Test_System.ipynb
│
├── dataset/
│   ├── images/
│   │   ├── train/
│   │   ├── val/
│   ├── labels/
│       ├── train/
│       ├── val/
│
├── models/
│   └── (weights .pt after training)

================================================================================
▶️ วิธีการทำงาน (Quick Start)
================================================================================
1. เปิด 01_SELECT_DATA.ipynb
   - กำหนด path, คลาสที่ต้องการ
   - Export list ไฟล์ที่ต้องการใช้งาน

2. เปิด 02_CREATE_DATA_FOR_TRAIN_YOLO.ipynb หรือ 02_CREATE_DATA_FOR_TRAIN_YOLO_2CLASS.ipynb
   - ตั้งค่า path
   - สร้างชุดข้อมูลสำหรับ Train/Test

3. ทำการเทรนโมเดล YOLO ปกติด้วยชุดข้อมูลที่สร้างขึ้น

4. เปิด 03.Test_System.ipynb
   - ใส่ path weight
   - ทดสอบโมเดลกับข้อมูลใหม่

================================================================================
💬 หมายเหตุสำคัญ
================================================================================
- ไฟล์ label ต้องอยู่ในฟอร์แมต YOLO: `class x_center y_center width height`
- ต้องใช้ไฟล์ภาพ `.jpg`, `.png` ที่ตรงกับ label
- ใช้ Python >= 3.8 และติดตั้งไลบรารี: numpy, pandas, opencv-python, ultralytics, tqdm

================================================================================
✅ จบกระบวนการทั้งหมด
================================================================================
